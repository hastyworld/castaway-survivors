# 표류 서바이버 (Castaway Survivors)

무인도에 표류한 생존자가 몰려오는 적을 자동 전투로 쓸어버리고, 살아남아 다음 섬으로 탈출하는 **뱀서라이크(서바이버) 게임**입니다.
기획안(`표류서바이버_기획안.pdf`)의 v1 스코프를 기준으로 만든 **플레이 가능한 초안(v0.1)** 입니다.

- 엔진: **Phaser 3** + **Vite** (100% 코드 → 바이브코딩에 최적)
- 화면: 세로(9:16), 모바일 우선
- 조작: 이동만 (공격은 자동)

---

## 1. 실행 방법 (PC 브라우저에서 바로 확인)

터미널(파워셸)에서 이 폴더로 이동한 뒤:

```powershell
npm install     # 최초 1회 (필요한 라이브러리 설치)
npm run dev      # 개발 서버 실행
```

- 실행되면 터미널에 `http://localhost:5173` 같은 주소가 뜹니다. 브라우저로 열면 게임이 나옵니다.
- **PC 조작**: 방향키 또는 W/A/S/D 로 이동.
- **모바일/터치**: 화면 아무 곳이나 누른 채로 드래그하면 그 자리에 조이스틱이 생깁니다.
- 같은 와이파이의 휴대폰에서도 접속해 실기기 손맛을 볼 수 있습니다(터미널의 `Network:` 주소 사용).

멈추려면 터미널에서 `Ctrl + C`.

---

## 2. 지금 들어있는 것 (기획안 대비)

| 기획안 단계 | 구현 여부 |
|---|---|
| 0. 이동 캐릭터 | ✅ 조이스틱 + 키보드 |
| 1~2. 자동공격 + 몰려오는 적 | ✅ 무기 3종 자동조준 / 적 5종 |
| 3. 경험치 & 레벨업 택1 | ✅ 3개 중 택1 팝업 |
| 4. 웨이브 전멸 | ✅ 섬당 4웨이브 |
| 5. 보스 | ✅ 섬마다 보스 1종 |
| 6. 섬/스테이지/탈출 | ✅ 섬 3개 + 탈것 진화 연출 |
| 7. 판 밖 영구 성장 & 저장 | ✅ 상점 + localStorage 저장 |
| 8. 광고 보상 | ⏳ (미구현 — 다음 단계) |
| 9. 아트·사운드 | ⏳ (지금은 임시 도형) |
| 10. 모바일 빌드 & 출시 | ⏳ (아래 3번 참고) |

**성장 3겹(기획안 5번)이 모두 들어있습니다.**
① 판 안 레벨업(무기/특성) · ② 판 밖 영구 성장(상점, 골드) · ③ 탈것 진화(뗏목→나룻배→범선).

### 밸런스/콘텐츠 수정 위치
- 적·무기·섬 수치: `src/content.ts`
- 화면 크기·색상·공용 상수: `src/config.ts`
- 무기 성능 공식: `src/systems/Weapons.ts`

---

## 3. 휴대폰에 설치하기 (안드로이드 APK)

**Capacitor 6**로 웹 게임을 안드로이드 앱으로 포장합니다. (JDK 17 + Android SDK 필요 — 이 PC엔 이미 설치됨)

### APK 만들기 (한 방에)
```powershell
./build-apk.ps1
```
- 끝나면 **바탕화면에 `표류서바이버.apk`** 가 생깁니다.
- 게임 코드를 고친 뒤 다시 이걸 실행하면 최신 APK가 새로 만들어집니다.

### 휴대폰에 설치
1. `표류서바이버.apk` 를 휴대폰으로 옮깁니다.
   - 가장 쉬운 방법: 바탕화면이 OneDrive라 **휴대폰 OneDrive 앱**에서 바로 내려받기, 또는 카카오톡 '나에게 보내기' / USB 케이블로 복사.
2. 휴대폰에서 그 APK 파일을 **탭** → "이 출처의 앱 설치 허용" 이 뜨면 허용.
3. Play Protect 경고(개발자 미확인)가 나오면 **"무시하고 설치"**. (내가 만든 앱이라 안전)
4. 설치 완료 → 앱 서랍에 **"표류 서바이버"** 아이콘.

### USB로 바로 설치 (개발자 옵션 켠 경우)
휴대폰 USB 디버깅을 켜고 케이블로 연결한 뒤:
```powershell
adb install -r "$env:USERPROFILE\OneDrive\바탕 화면\표류서바이버.apk"
```

> ⚠️ 이건 **디버그(테스트) APK** 입니다. 내 폰/지인 테스트용으로 충분하고, 실제 구글 플레이 출시 때는 별도의 **서명된 릴리스 빌드(AAB)** 와 개발자 계정($25)이 필요합니다.

### 구글 플레이 정식 출시 때 추가로 필요한 것
- 구글 플레이 개발자 계정($25, 1회)
- 앱 아이콘 / 스플래시 / 스토어 스크린샷
- 릴리스 서명 키(keystore) 로 서명한 **AAB** 빌드
- 보상형 광고는 AdMob 플러그인으로 **맨 마지막에** 연동 (기획안 7·8번: 필수 광고 0개)

---

## 4. 다음에 만들면 좋은 것
- 8단계: 가짜 광고 버튼 → 아이템 쿠폰(C/B/A) 지급 로직
- 9단계: 임시 도형을 AI 스프라이트로 교체, 효과음/타격감
- 아이템 인벤토리(장착형 영구 아이템, C/B/A 등급)
